FLASK_ENV=development
SECRET_KEY=change-this-in-production
# DATABASE_URL=sqlite:///absolute/path/to/app.db  # optional override; defaults to <project_root>/app.db
# SESSION_COOKIE_SECURE=True  # set to True only if served over HTTPS (e.g. behind a TLS reverse proxy)
